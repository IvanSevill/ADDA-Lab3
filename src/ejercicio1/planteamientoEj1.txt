// DATOS DE ENTRADA
// n: entero que corresponde al número de productos
// m: entero que corresponde al número de almacenes
// Iij: i es incompatible con j

// VARIABLES
// Xij: si se envia el elemento i al almacen j : i=0,1,...,n-1, j=0,1,...,m-1

// FUNCION OBJETIVO
// min sum(Xij) : i=0,1,...,n-1, j=0,1,...,m-1

// RESTRICCIONES
// 1. sum(Xij, i in 0..m-1) <= tamañoAlmacen(j)
// 2. sum(Xij, i in 0..m-1) <= tamañoAlmacen(j)


// int Xi, i = 0,1,...,m-1